# Dependencias del entorno aislado .venv-noise usado únicamente por main.py.
#
# spanish_nlp arrastra spacy 2.3.9/thinc 7.4.6, que solo tienen wheels
# precompiladas para numpy<2 — eso choca con el numpy 2.x que ya usan
# torch/scikit-learn/pandas en el venv compartido del workspace de uv, por
# eso este script corre en su propio venv en vez del venv del proyecto.
#
# Setup (una sola vez):
#   uv venv .venv-noise --python 3.11
#   uv pip install --python .venv-noise -r requirements-noise.txt
#
# Ejecutar el generador:
#   .venv-noise/Scripts/python.exe main.py        (Windows)
#   .venv-noise/bin/python main.py                (Linux/Mac)

numpy<2
spacy==2.3.9
thinc==7.4.6
spanish_nlp
unidecode
pyspellchecker
polars
